Use `python3`

Install dependencies:
```
python -m venv venv
source venv/bin/activate        # On Linux/macOS
# OR
venv\Scripts\activate           # On Windows

pip install -r requirements.txt
```

Run
```
python build_bipartite_graph.py student_depression_dataset.csv
```
